package by.runets.vehiclescrapper.persistence.converter

/*
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.core.convert.converter.Converter;
import org.springframework.data.convert.ReadingConverter;

import java.io.IOException;
import java.util.HashMap;
import java.util.Map;

@ReadingConverter
class JsonToMapConverter : Converter<Json, Map<String, Object>> {
    private val objectMapper: ObjectMapper? = null

    override fun convert(json: Json): Map<String, Object>? {
        try {
            return objectMapper.readValue(json.asString(), object : TypeReference() {})
        } catch (e: IOException) {
        }
        return HashMap<Any, Any>()

    }
}*/
